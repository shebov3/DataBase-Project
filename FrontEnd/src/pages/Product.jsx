import React from "react";

const Product = ({ gameId }) => {
  
  return (
    <div className="text-red-700">
      <h1>This is Game with game id = {gameId}</h1>
    </div>
  );
};

export default Product;